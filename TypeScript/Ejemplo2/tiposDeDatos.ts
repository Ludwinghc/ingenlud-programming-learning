// * Tipos básicos de datos en TS
// * Tipo String o Cadena de texto
let cadenaTexto: String; //Es una forma de definir una variable de tipo String

cadenaTexto = "Ludwing Hernandez"; // Puede contener n numero de caracteres

console.log(cadenaTexto);

//* Tipo Number o numero
let num: number;

num = 20;

console.log(num);

//* Tipo Booleano (true/false)
let ask: boolean;

ask = true;

console.log(ask);

//*Tipo null (No tiene valor intencionalmente)
let nombre: string | null;

//* Tipo undefined (Aun no ha sido asignado)
let edad: number | undefined;